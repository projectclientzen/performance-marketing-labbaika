import { ProgramManager } from "@/components/ProgramManager";

// Program & Harga dipakai CS untuk upsell/cross-sell (akses tulis dibuka di
// migrasi 029). Komponennya sama persis dengan yang dipakai owner.
export default function CsProgramsPage() {
  return <ProgramManager />;
}
