# db/

Generated TypeScript types and DB-adjacent helpers that aren't pure utils (those live in `lib/`).

`supabase gen types typescript` output lands here once the schema exists (see `05-TASKS-ClaudeCode-Major.md` CC-B02 onward).
